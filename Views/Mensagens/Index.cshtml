@model List<Mensagem>

<h2>Mensagens</h2>

<table class="table">
    <thead>
        <tr>
            <th>Conteúdo</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var mensagem in Model)
        {
            <tr>
                <td>@mensagem.Conteudo</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@mensagem.Id">Editar</a> |
                    <a asp-action="Delete" asp-route-id="@mensagem.Id">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Create">Nova Mensagem</a>
